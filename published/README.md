# Published Presentations

Real decks and reports generated with Presentation Forge, by 18deepnar@gmail.com.

These are committed intentionally so visitors can see the product without logging in. They are sanitized only to the extent of being my own work — no other user's decks are included.

| File | Description |
|---|---|
| `recent-trends-mixed-mode-presentation.pptx` | 12 slides — Recent Trends in Mixed-Mode Programming |
| `recent-trends-mixed-mode-report.docx` | Companion report (DOCX template) |
| `first-impressions-networking-presentation.pdf` | First Impressions — PDF export |
| `first-impressions-networking-presentation.pptx` | First Impressions — PPTX |
| `first-impressions-networking-report.docx` | First Impressions — Report DOCX |
| `first-impressions-topic2-presentation.pptx` | First Impressions variant 2 — PPTX |

Generated via `npm run forge -- new ...` → `generate` → `npm run render` + `npm run preview`. Private decks remain in `decks/<slug>/` (gitignored).
